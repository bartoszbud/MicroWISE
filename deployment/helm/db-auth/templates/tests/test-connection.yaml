apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "db-auth.fullname" . }}-test-connection"
  labels:
    {{- include "db-auth.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "db-auth.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
