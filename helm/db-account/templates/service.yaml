apiVersion: v1
kind: Service
metadata:
  name: {{ include "db-account.fullname" . }}
  labels:
    {{- include "db-account.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "db-account.selectorLabels" . | nindent 4 }}
