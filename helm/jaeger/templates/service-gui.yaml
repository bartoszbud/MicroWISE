apiVersion: v1
kind: Service
metadata:
  name: {{ include "jaeger.fullname" . }}
  labels:
    {{- include "jaeger.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service_gui.type }}
  ports:
    - port: {{ .Values.service_gui.port }}
      targetPort: {{ .Values.service_gui.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "jaeger.selectorLabels" . | nindent 4 }}
